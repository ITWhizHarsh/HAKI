# HAKI Core — API Keys (TEMPLATE)
# Copy this file to `Core/.env` and fill in real values.
# Core/.env is the SINGLE source of truth for keys and is git-ignored.
# Loaded automatically by load_dotenv() in haki_core_service.py and by start_haki.sh.

# ── LLM Cloud APIs (chat / reasoning) ────────────────────────────────────────
# Primary tier. Get one at https://console.groq.com/keys  (format: gsk_...)
HAKI_GROQ_API_KEY=__REDACTED_LEGACY_SECRET__
# Fallback tier. https://cloud.cerebras.ai  (format: csk-...)
HAKI_CEREBRAS_API_KEY=__REDACTED_LEGACY_SECRET__
# Fallback tier. https://aistudio.google.com/app/apikey  (format: AIza...)
HAKI_GEMINI_API_KEY=__REDACTED_LEGACY_SECRET__

# ── STT Cloud API (speech-to-text) ───────────────────────────────────────────
# Used when no local STT model is installed. https://console.deepgram.com
HAKI_DEEPGRAM_API_KEY=__REDACTED_LEGACY_SECRET__

# ── TTS Cloud API (text-to-speech) ───────────────────────────────────────────
# Used as a fallback when local Kokoro TTS is unavailable. https://play.cartesia.ai
HAKI_CARTESIA_API_KEY=__REDACTED_LEGACY_SECRET__

# ── Obsidian Vault Path (local memory store) ─────────────────────────────────
HAKI_OBSIDIAN_VAULT=/Users/youruser/Obsidian/HAKI_Brain

# ── Memory Processing Pipeline (Architect's Hybrid Pipeline) ────────────────
# Minutes between raw/ folder processing runs. Default: 30. Valid range: 1-1440.
HAKI_PIPELINE_RAW_INTERVAL_MINUTES=30
# Daily time (HH:MM, 24h, local timezone) conversations/ logs are processed. Default: 02:00.
HAKI_PIPELINE_CONV_RUN_TIME=02:00
# Available system memory (MB) below which a Heavy Pass run is suspended. Default: 500.
HAKI_PIPELINE_LOW_MEMORY_THRESHOLD_MB=500
# Seconds to wait for the Bonsai-8B LLM before aborting a Heavy Pass run. Default: 30.
HAKI_PIPELINE_LLM_TIMEOUT_SECS=30

# ── TTS Voice Tuning (edge-tts) ──────────────────────────────────────────────
# Voice id, speech-rate delta (%) and pitch delta (Hz). A bare number is
# treated as positive (e.g. 32% → +32%). Use a leading - to slow/lower.
HAKI_TTS_VOICE=hi-IN-MadhurNeural
HAKI_TTS_RATE=+32%
HAKI_TTS_PITCH=+17Hz
