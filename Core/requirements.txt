# HAKI Core — pinned production dependencies for Python 3.11 / Apple Silicon M2
# Install: pip install -r requirements.txt
# The generated transitive lock is requirements.lock.
# Text-only cloud, memory, ingestion, and automation integrations are opt-in
# extras in pyproject.toml; they are intentionally not voice runtime dependencies.

# ── Core / IPC ───────────────────────────────────────────────────────────────
grpcio==1.64.1
grpcio-tools==1.64.1
protobuf==5.29.6
PyYAML==6.0.3
python-dotenv==1.0.1

# ── Realtime local voice runtime ─────────────────────────────────────────────
# Keep every direct pin synchronized with pyproject.toml and requirements.lock.
pipecat-ai==1.4.0
mlx-lm==0.18.1
TTS==0.22.0
silero-vad==5.1.2
torch==2.5.1
torchaudio==2.5.1
soundfile==0.12.1
numpy==1.26.4
psutil==6.0.0
pydantic==2.8.2

# ── Testing ──────────────────────────────────────────────────────────────────
hypothesis==6.103.2
pytest==8.2.2
pytest-asyncio==0.23.7
pytest-cov==5.0.0
